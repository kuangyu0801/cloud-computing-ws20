server:
  # assets are served from "/*", JAX-RS resources from "/api/*"
  rootPath: /api/*

logging:
  level: INFO
  loggers:
    de.ustutt.iaas.cc: DEBUG

swagger:
  resourcePackage: de.ustutt.iaas.cc.resources

# optional
#serviceInstanceID: service 1